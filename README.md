README - JMBAG project (SQLi + CSRF)

Implemented (simulated):
- SQL Injection (tautology)
- CSRF (Cross-Site Request Forgery)

How to run:
1. npm install
2. npm start
3. Open http://localhost:3000

Notes:
- All behavior is safe simulation for education.
- For SQL test use sample payload: 'OR 1=1 #
- For CSRF legit flow use token: securetoken123
